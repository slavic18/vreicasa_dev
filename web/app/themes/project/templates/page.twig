{% extends 'base.twig' %}
{% block content %}
    <div class="post-title">
    <h3 class="section-title vinous">{{ post.title }}</h3>
    </div>
    <div class="post-content">
        <div class="container">
            <article class="post-content-description">
                {{ post.content }}
            </article>
        </div>
    </div>
    {% include 'partials/sections/subscribe-form.twig' %}

{% endblock %}